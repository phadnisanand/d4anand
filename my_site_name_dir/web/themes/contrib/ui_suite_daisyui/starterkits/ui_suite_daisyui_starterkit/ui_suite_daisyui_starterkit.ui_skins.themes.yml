# No value because same as plugin ID.
cupcake:
  enabled: false

# No value because same as plugin ID.
aqua:
  enabled: false

# No value because same as plugin ID.
black:
  enabled: false

# No value because same as plugin ID.
bumblebee:
  enabled: false

# No value because same as plugin ID.
cmyk:
  enabled: false

# No value because same as plugin ID.
corporate:
  enabled: false

# No value because same as plugin ID.
cyberpunk:
  enabled: false

# No value because same as plugin ID.
dracula:
  enabled: false

# No value because same as plugin ID.
emerald:
  enabled: false

# No value because same as plugin ID.
fantasy:
  enabled: false

# No value because same as plugin ID.
forest:
  enabled: false

# No value because same as plugin ID.
garden:
  enabled: false

# No value because same as plugin ID.
halloween:
  enabled: false

# No value because same as plugin ID.
lofi:
  enabled: false

# No value because same as plugin ID.
luxury:
  enabled: false

# No value because same as plugin ID.
pastel:
  enabled: false

# No value because same as plugin ID.
retro:
  enabled: false

# No value because same as plugin ID.
synthwave:
  enabled: false

# No value because same as plugin ID.
valentine:
  enabled: false

# No value because same as plugin ID.
wireframe:
  enabled: false

# No value because same as plugin ID.
autumn:
  enabled: false

# No value because same as plugin ID.
business:
  enabled: false

# No value because same as plugin ID.
acid:
  enabled: false

# No value because same as plugin ID.
lemonade:
  enabled: false

# No value because same as plugin ID.
night:
  enabled: false

# No value because same as plugin ID.
coffee:
  enabled: false

# No value because same as plugin ID.
winter:
  enabled: false

# No value because same as plugin ID.
dim:
  enabled: false

# No value because same as plugin ID.
nord:
  enabled: false

# No value because same as plugin ID.
sunset:
  enabled: false

# No value because same as plugin ID.
caramellatte:
  enabled: false

# No value because same as plugin ID.
abyss:
  enabled: false

# No value because same as plugin ID.
silk:
  enabled: false
